# 시스템 트레이 기능 추가 완료

## 변경사항

### 1. 시스템 트레이 유틸리티 (`electron/utils/tray.js`)
- 트레이 아이콘 생성 및 관리
- 컨텍스트 메뉴 (열기, 종료)
- 읽지 않은 메시지 개수 표시
- 트레이 아이콘 클릭으로 윈도우 표시/숨김

### 2. 메인 프로세스 업데이트 (`electron/main.js`)
- 트레이 통합
- 윈도우 닫기 버튼 클릭 시 트레이로 최소화 (종료하지 않음)
- 앱 종료는 트레이 메뉴에서만 가능
- 백그라운드 실행 지원

## 사용 방법

1. **윈도우 닫기**: X 버튼 클릭 시 트레이로 최소화
2. **앱 열기**: 
   - 트레이 아이콘 클릭
   - 트레이 메뉴 > "열기" 선택
3. **앱 종료**: 트레이 메뉴 > "종료" 선택

## 기능

✅ 시스템 트레이에 아이콘 표시
✅ 트레이 메뉴 (열기, 종료)
✅ 윈도우 닫기 시 백그라운드 실행
✅ 읽지 않은 메시지 개수 표시 (향후 WebSocket 연동 시 활성화)
✅ 트레이 아이콘 클릭으로 윈도우 토글

## 다음 단계

- WebSocket 연동 시 실시간 읽지 않은 메시지 개수 업데이트
- 새 메시지 도착 시 트레이 알림
