# 📱 메신저 앱 개발 가이드 (3Dcommu 백엔드 연동)

이 문서는 3Dcommu 백엔드 서버와 연동하여 메신저 앱을 개발할 때 필요한 API 엔드포인트와 데이터 구조를 정의합니다.

---

## 1. 사전 준비 (인증)
모든 API 호출에는 로그인 후 발급받은 **JWT 토큰**이 필요합니다.
- **Header:** `Authorization: Bearer {JWT_TOKEN}`
- **CORS 허용:** `http://localhost:5173` 및 `http://localhost:3000`이 허용되어 있습니다.

---

## 2. 유저 정보 조회 (재화 및 프로필)
`205PreDev`와 같은 특정 유저의 상태(금화, 은화, 프로필 이미지)를 가져옵니다.

- **엔드포인트:** `GET /api/auth/me` (내 정보) 또는 `GET /api/profile`
- **주요 응답 데이터:**
    - `username`: 유저 닉네임 (예: "205PreDev")
    - `goldCoins`: 보유 중인 금화
    - `silverCoins`: 보유 중인 은화
    - `selectedProfile`: 현재 장착 중인 프로필 아이템 정보
    - `selectedOutline`: 현재 장착 중인 테두리 아이템 정보

---

## 3. 친구 목록 관리
메신저의 기본이 되는 친구 목록을 가져오고 관리합니다.

- **친구 목록 조회:** `GET /api/friends`
    - 상태가 `ACCEPTED`인 친구 리스트만 반환됩니다.
- **친구 검색:** `GET /api/friends/search?username={닉네임}`
- **요청/수락/거절:**
    - 요청: `POST /api/friends/request` (Body: `{"username": "상대방"}`)
    - 수락: `POST /api/friends/accept/{friendshipId}`
    - 삭제: `DELETE /api/friends/{friendshipId}`

---

## 4. 1:1 대화 (DM) 기능
채팅 기록 조회 및 메시지 전송 로직입니다.

- **대화방 목록:** `GET /api/messages/conversations`
    - 마지막 메시지와 대화 상대방 정보를 포함한 리스트를 가져옵니다.
- **대화 기록 조회:** `GET /api/messages/dm/{friendId}?limit=50`
    - `friendId`는 유저의 고유 ID(Long)입니다.
- **메시지 전송:** `POST /api/messages/dm`
    - **Body:**
      ```json
      {
        "receiverId": 123,
        "content": "안녕하세요!"
      }
      ```
- **읽음 처리:** `POST /api/messages/mark-read/{friendId}`

---

## 5. 실시간 채팅 (WebSocket)
실시간 메시지 수신을 위해 STOMP 프로토콜을 사용합니다.

- **연결 주소:** `ws://localhost:8080/ws`
- **DM 구독 경로:** `/user/queue/messages`
- **광장 채팅 구독 경로:** `/topic/public`

---

## 6. 개발 시 주의사항
1. **ID값 구분:** 유저 검색은 `username`(String)으로 하지만, 대화 기록 조회나 친구 수락은 `id`(Long)값을 사용합니다.
2. **날짜 포맷:** 서버에서 내려주는 시간 값은 ISO-8601 형식이므로 클라이언트에서 적절히 포맷팅이 필요합니다.
3. **재화 업데이트:** 메신저 내에서 아이템 구매나 재화 변동이 생길 경우, `User` 엔티티의 `gold_coins`와 `silver_coins`를 다시 불러와 동기화해야 합니다.

---
*최종 업데이트: 2025-12-29*
